import React, { useState, useEffect } from 'react'
import { Menu, X } from 'lucide-react'
const NAV_ITEMS = [{ label: 'Overview', href: '#hero' }, { label: 'Specs', href: '#specs' }, { label: 'Features', href: '#features' }, { label: 'Modular', href: '#modular' }, { label: 'Build', href: '#build' }, { label: 'MK2', href: '#mk2' }]
export default function Navbar() { const [scrolled, setScrolled] = useState(false); const [open, setOpen] = useState(false); useEffect(() => { const onScroll = () => setScrolled(window.scrollY > 40); window.addEventListener('scroll', onScroll); return () => window.removeEventListener('scroll', onScroll) }, []); return <nav className={`fixed top-0 left-0 right-0 z-50 transition-all duration-500 ${scrolled ? 'bg-[#0a0e17]/90 backdrop-blur-xl border-b border-cyan-500/10 shadow-lg shadow-cyan-500/5' : 'bg-transparent'}`}><div className="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between"><a href="#hero" className="flex items-center gap-2.5 group"><div className="w-8 h-8 rounded-lg bg-gradient-to-br from-cyan-400 to-blue-600 flex items-center justify-center text-xs font-black tracking-wider">M</div><span className="font-heading font-bold text-lg tracking-[0.2em] text-white group-hover:text-cyan-400">MARS</span><span className="text-[10px] font-mono text-cyan-500/70 mt-1">Mk1</span></a><div className="hidden md:flex items-center gap-1">{NAV_ITEMS.map(item => <a key={item.href} href={item.href} className="px-4 py-2 text-xs font-medium tracking-widest uppercase text-white/50 hover:text-cyan-400">{item.label}</a>)}<a href="/" className="ml-3 px-5 py-2 rounded-full bg-gradient-to-r from-cyan-500 to-blue-600 text-xs font-semibold tracking-wider uppercase">Launch App</a></div><button onClick={() => setOpen(!open)} className="md:hidden w-10 h-10 flex items-center justify-center text-white/70">{open ? <X size={20} /> : <Menu size={20} />}</button></div>{open && <div className="md:hidden bg-[#0a0e17]/95 backdrop-blur-xl border-t border-cyan-500/10 px-6 pb-6 pt-2">{NAV_ITEMS.map(item => <a key={item.href} href={item.href} onClick={() => setOpen(false)} className="block py-3 text-sm tracking-widest uppercase text-white/60 border-b border-white/5">{item.label}</a>)}<a href="/" className="block py-3 text-sm tracking-widest uppercase text-cyan-400 font-semibold">Launch App →</a></div>}</nav> }
